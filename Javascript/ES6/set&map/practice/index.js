// 1. 某次活动中，系统收集的学生学号存在重复，请你使用 Set 去重，并得到去重后的数组。
// 请将其去重，得到： [101, 102, 103, 104]
// 原始数组如下：
const ids = [101, 102, 102, 103, 104, 104, 104];

// 2.网站需要记录用户是否已经访问过某个页面。
// 请使用 Set 完成下列功能：

// - 访问 id=1（加入 Set）
// - 再次访问 id=1（可以判断“已经访问过”）
// - 访问 id=2

// 请输出用户访问过的页面 ID。

// 3.请找出两个班里 同时出现的学生姓名（交集）。
// 两个学生名单如下：

const classA = ['张三', '李四', '王五'];
const classB = ['李四', '赵六', '王五'];

// 4.一句话如下：
// 请你使用  Map  统计每个单词出现了几次。

// 打印结果：
// apple → 3 次
// banana → 2 次
// orange → 1 次

const words = ['apple', 'banana', 'apple', 'orange', 'banana', 'apple'];

// 5.请你创建两个对象作为 key，并用 Map 记录它们的分数。
// 要求用 Map 来保存：

// - student1 → 分数 90
// - student2 → 分数 85

// 并打印出 student1 的分数。

const student1 = { id: 1 };
const student2 = { id: 2 };
