/**
 * 【题目：点赞按钮（防连点）】
 *
 * 场景：
 * 你在做一个“点赞”按钮。产品要求：用户点完一次后，必须等一会儿才能再点，
 * 防止疯狂连点。
 *
 * 规则（必须严格实现）：
 * 1) 初始点赞数为 0
 * 2) 点击“点赞”按钮：
 *    - 点赞数 +1
 *    - 按钮立即变为禁用状态
 *    - 提示文案变为：“冷却中…”
 * 3) 冷却结束（2 秒后）：
 *    - 按钮恢复可点击
 *    - 提示文案变为：“可以继续点赞”
 *
 * 你不需要自己写复杂 UI，按下面文字即可：
 * - 按钮文字固定写：👍 点赞
 * - 提示区域用 <p className="small">...</p>
 *
 * 提示：
 * - 你需要两个 state：likes（数字） + cooling（布尔）
 * - 可以用 setTimeout 实现 2 秒冷却（允许用，但不需要 useEffect）
 */
